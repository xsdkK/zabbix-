`在实验2中已经完成了Zabbix-server、Zabbix-web-mysql、Zabbix-apache-conf和Zabbix-agent,但是当时Zabbix只能监控那一台服务器，在网页前端也只能查看到那一台服务器的基础数据，在这个实验中将进行另外一台服务器的添加，实现一个zabbix server监控多个zabbix-agent`

**步骤一：确认端口**

    确定VPC内10050（Agent监听端口）和10051（Zabbix Server接收agent数据端口）正确放行
    注：由于我的两台服务器都是处于同一VPC的，因为我在实验二中已经做过端口放行了，所以无需再进行端口配置。不同的情况可视情况而定。

**步骤二：在新加入服务器上安装Zabbix Agent**

    rpm -ivh https://repo.zabbix.com/zabbix/6.0/rhel/8/x86_64/zabbix-release-6.0-4.el8.noarch.rpm
    yum install zabbix-agent -y

**步骤三：修改Agent**

    1.进入存放zabbix_agentd.conf的目录（/etc），进行zabbix_agentd.conf的修改
    //先cd，后vi

    2.修改三个核心参数
        Server=Zabbix服务端公网IP(即新加入的服务器)
        ServerActive=Zabbix服务端公网IP:10051
        当前被监控主机名称（自定义，Web端添加主机时要一模一样）：Hostname=LLM-run-Cloud

**步骤四：启动并开机自启(在被监控服务器端)**

    systemctl start zabbix-agentd
    systemctl enable zabbix-agentd
    systemctl status zabbix-agentd

**步骤五：Web端添加被监控主机**

    1.登录Zabbix网页端（详情见实验2）
    2.点击左侧菜单：配置->主机->创建主机
![](图片示例集/zabbix%20web端添加监控主机示例.png)
`注意：主机名称一定要和步骤三中的Hostname的值一模一样，不然无法添加成功`
`可见的名称可忽略，模板可先选择Template OS Linux by Zabbix agent（后续可按需求添加），群组也是按需选择`
`Agent IP地址填入刚才修改配置文件的服务器地址，端口选择10050`
    3.点击添加保存

**步骤六：验证连通性**

    在Zabbix-Server端测试能否连通客户端10050：
    telnet Zabbix-Agent公网IP 10050

    在客户端测试能否链接服务端10051：
    telnet Zabbix-Server公网IP 10051

    能够在zabbix web主机里看到刚才已经添加成果的主机

# 成果展示

//主机数量那里为2
![](图片示例集/Zabbix%20添加主机结果.png)
